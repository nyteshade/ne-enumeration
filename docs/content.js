;(function() {
/**
 * DocEntry and DocContent — the documentation data model.
 *
 * This file is inlined into the generated content.js output.
 * It must be self-contained (no imports) and ES5-compatible
 * so it works in any browser without transpilation.
 *
 * DocEntry wraps every documented item (pages and members alike).
 * DocContent wraps the entire documentation set and provides
 * query methods over it.
 */

/* eslint-disable no-var */
/* istanbul ignore file */

function DocEntry(data, parent) {
  var self = this;
  var key;

  // Copy all data properties onto this instance
  for (key in data) {
    if (Object.prototype.hasOwnProperty.call(data, key) && key !== 'members') {
      self[key] = data[key];
    }
  }

  // Parent back-reference (not enumerable to avoid circular JSON issues)
  Object.defineProperty(self, 'parent', {
    value: parent || null,
    writable: true,
    enumerable: false,
    configurable: true,
  });

  // Recursively wrap members as DocEntry instances
  var rawMembers = data.members || {};
  var members = {};
  var categories = [
    'classes', 'methods', 'staticMethods',
    'properties', 'staticProperties',
    'events', 'typedefs'
  ];

  for (var i = 0; i < categories.length; i++) {
    var cat = categories[i];
    var items = rawMembers[cat] || [];
    members[cat] = [];
    for (var j = 0; j < items.length; j++) {
      members[cat].push(new DocEntry(items[j], self));
    }
  }

  self.members = members;
}

/** All members flattened into a single array */
Object.defineProperty(DocEntry.prototype, 'allMembers', {
  get: function () {
    var result = [];
    var members = this.members || {};
    var keys = Object.keys(members);
    for (var i = 0; i < keys.length; i++) {
      var arr = members[keys[i]];
      for (var j = 0; j < arr.length; j++) {
        result.push(arr[j]);
      }
    }
    return result;
  },
  enumerable: true,
  configurable: true,
});

/** Whether this entry is callable (has a function signature) */
Object.defineProperty(DocEntry.prototype, 'isCallable', {
  get: function () {
    return this.signature !== null && this.signature !== undefined;
  },
  enumerable: true,
  configurable: true,
});

/** Whether this entry has any child members */
Object.defineProperty(DocEntry.prototype, 'hasMembers', {
  get: function () {
    return this.allMembers.length > 0;
  },
  enumerable: true,
  configurable: true,
});

/**
 * Find a member by name, searching deeply through all
 * nested members.
 *
 * @param {string} name - The member name to find.
 * @returns {DocEntry|null} The matching entry, or null.
 */
DocEntry.prototype.find = function find(name) {
  var members = this.allMembers;
  for (var i = 0; i < members.length; i++) {
    if (members[i].name === name) return members[i];
    var deep = members[i].find(name);
    if (deep) return deep;
  }
  return null;
};

/**
 * Return all members matching a given kind.
 *
 * @param {string} kind - The kind to filter by (e.g., 'function', 'member').
 * @returns {DocEntry[]}
 */
DocEntry.prototype.membersByKind = function membersByKind(kind) {
  return this.allMembers.filter(function (m) { return m.kind === kind; });
};

/**
 * Walk up the parent chain and return the ancestry as an array,
 * starting from the root and ending at this entry.
 *
 * @returns {DocEntry[]}
 */
DocEntry.prototype.ancestry = function ancestry() {
  var chain = [this];
  var current = this;
  while (current.parent) {
    chain.unshift(current.parent);
    current = current.parent;
  }
  return chain;
};

// ──────────────────────────────────────────────────────────────

function DocContent(raw) {
  this.themeVersion = raw.themeVersion || null;
  this.generatedAt = raw.generatedAt || null;
  this.packageInfo = raw.packageInfo || null;
  this.readme = raw.readme || '';
  this.nav = raw.nav || [];

  // Pre-highlighted source files
  this.sources = raw.sources || {};

  // Wrap every page in DocEntry
  this.pages = {};
  var rawPages = raw.pages || {};
  var slugs = Object.keys(rawPages);
  for (var i = 0; i < slugs.length; i++) {
    this.pages[slugs[i]] = new DocEntry(rawPages[slugs[i]], null);
  }
}

/**
 * Get a page by slug.
 *
 * @param {string} slug
 * @returns {DocEntry|null}
 */
DocContent.prototype.page = function page(slug) {
  return this.pages[slug] || null;
};

/**
 * Get pre-highlighted source HTML for a file.
 *
 * @param {string} file - The source file short path.
 * @returns {string|null} The highlighted HTML, or null.
 */
DocContent.prototype.source = function source(file) {
  if (!file) return null;

  // Exact match
  if (this.sources[file]) return this.sources[file];

  // Fallback: match by basename or suffix (handles path prefix mismatches
  // between AST-relative paths and JSDoc shortpaths)
  var keys = Object.keys(this.sources);
  var fileLower = file.toLowerCase();
  var sep = file.lastIndexOf('/');
  var basename = sep >= 0 ? file.substring(sep + 1) : file;

  // Try suffix match (e.g., "descriptor.utils.js" matches "utils/descriptor.utils.js")
  for (var i = 0; i < keys.length; i++) {
    if (keys[i].toLowerCase().endsWith(fileLower) || fileLower.endsWith(keys[i].toLowerCase())) {
      return this.sources[keys[i]];
    }
  }

  // Try basename match (last resort)
  var basenameLower = basename.toLowerCase();
  for (var j = 0; j < keys.length; j++) {
    var keySep = keys[j].lastIndexOf('/');
    var keyBase = keySep >= 0 ? keys[j].substring(keySep + 1) : keys[j];
    if (keyBase.toLowerCase() === basenameLower) {
      return this.sources[keys[j]];
    }
  }

  return null;
};

/**
 * Return all available source file paths.
 *
 * @returns {string[]}
 */
DocContent.prototype.sourceFiles = function sourceFiles() {
  return Object.keys(this.sources);
};

/**
 * Search across all pages and their members by name or description.
 *
 * @param {string} query - The search string.
 * @returns {{ entry: DocEntry, pageSlug: string }[]}
 */
DocContent.prototype.search = function search(query) {
  var q = query.toLowerCase();
  var results = [];
  var slugs = Object.keys(this.pages);

  for (var i = 0; i < slugs.length; i++) {
    var slug = slugs[i];
    var entry = this.pages[slug];

    if (entry.name && entry.name.toLowerCase().indexOf(q) !== -1) {
      results.push({ entry: entry, pageSlug: slug, anchor: null });
    }
    if (entry.description && entry.description.toLowerCase().indexOf(q) !== -1) {
      results.push({ entry: entry, pageSlug: slug, anchor: null });
    }

    // Search members recursively
    var stack = entry.allMembers.slice();
    while (stack.length > 0) {
      var m = stack.pop();
      var nameMatch = m.name && m.name.toLowerCase().indexOf(q) !== -1;
      var descMatch = m.description && m.description.toLowerCase().indexOf(q) !== -1;
      if (nameMatch || descMatch) {
        results.push({ entry: m, pageSlug: slug, anchor: m.name });
      }
      var children = m.allMembers;
      for (var j = 0; j < children.length; j++) {
        stack.push(children[j]);
      }
    }
  }

  return results;
};

/**
 * Return all pages matching a given kind.
 *
 * @param {string} kind
 * @returns {DocEntry[]}
 */
DocContent.prototype.byKind = function byKind(kind) {
  var result = [];
  var slugs = Object.keys(this.pages);
  for (var i = 0; i < slugs.length; i++) {
    if (this.pages[slugs[i]].kind === kind) {
      result.push(this.pages[slugs[i]]);
    }
  }
  return result;
};

/**
 * Return all nav group titles that have items.
 *
 * @returns {string[]}
 */
DocContent.prototype.sections = function sections() {
  var result = [];
  for (var i = 0; i < this.nav.length; i++) {
    if (this.nav[i].items && this.nav[i].items.length > 0) {
      result.push(this.nav[i].title);
    }
  }
  return result;
};


var raw = {"themeVersion":"2.1.2","generatedAt":"2026-03-09T23:01:07.806Z","packageInfo":{},"nav":[{"title":"Classes","items":[{"name":"Enumeration","slug":"Enumeration"}]},{"title":"Functions","items":[{"name":"SubscriptProxy","slug":"SubscriptProxy"}]},{"title":"Objects","items":[{"name":"is","slug":"is"}]},{"title":"Type Definitions","items":[{"name":"TypeToolbox","slug":"TypeToolbox"}]}],"pages":{"home":{"slug":"home","kind":"home","name":"Home","title":"Home"},"Enumeration":{"slug":"Enumeration","name":"Enumeration","kind":"class","title":"Enumeration","source":{"file":"enumeration.mjs","line":172},"description":"<p>Allows the creation of static, complex, enumeration values\nthat can be used and identified at runtime (as opposed to\nTypeScript enum values which apply only during compile time).</p>\n<p>Below is a simple example defining some colors on a <code>Color</code>\nenum. Simple enums are a combination of key and value. In\nthe following case the key is the color name and the value\nis an object with color channel data (red, green, blue, and\nalpha channel values from 0 to 255).</p>\n<pre class=\"prettyprint source lang-js\"><code>class Color extends Enumeration {\n  static {\n    Color.define('red', {r: 255, g: 0, b: 0, a: 255})\n    Color.define('green', {r: 0, g:255, b: 0, a: 255})\n    Color.define('blue', {r: 0, g: 0, b: 255, a: 255})\n  }\n}\n\nColor.red.value.r === 255 // true\nColor.red.r === 255 // true; this uses the SubscriptProxy\n\n// Grab all four color channels' data in one go. This also\n// uses the SubscriptProxy for syntactic sugar.\nconst { r, g, b, a } = Color.red\n</code></pre>\n<p>New types could be defined later with subsequent Color.define()\ncalls. But take the case of customizable color case such as\none that is different everytime it is used. Each instance of a\nchild class of an Enumeration has the option to associate values\nto it.</p>\n<pre class=\"prettyprint source lang-js\"><code>class Color extends Enumeration {\n  static {\n    // assume cases from above as well\n\n    // define an `rgb` case that assumes associated color\n    // channel data.\n    Color.define('rgb', {r: 0, g: 0, b: 0, a: 255})\n  }\n}\n\nlet car = { model: &quot;Ferrari&quot;, color: Color.red }\nlet horse = {\n  breed: &quot;American Saddlebred&quot;,\n  color: Color.rgb.associate({ r: 200, g: 76, b: 49, a: 255 })\n}\n</code></pre>\n<p>This last snippet of code creates a contrived horse object with\na chestnut/sorrel colored color. The enum case type for this is\nstill <code>Color.rgb</code>, but a rgb cased enum indicates at least three\ncolor channels. It's specific values are available through\nthe SubscriptProxy's direct key value access, .r/.g/.b/.a, but\nis stored not in the value but in the associations object. If we\nwanted <code>Color.rgb</code> to directly surface its associated values, a\nlittle more customization would be needed.</p>\n<pre class=\"prettyprint source lang-js\"><code>class Color extends Enumeration {\n  static {\n    // assume cases from above as well\n\n    // define an `rgb` case that assumes associated color\n    // channel data.\n    Color.define('rgb', {r: 0, g: 0, b: 0, a: 255}, {\n      // always return the associations object instead of the\n      // value for an unmodified Color enum case.\n      get value() { return this.associations }\n    })\n  }\n}\n\n// Now, you can access it in all three ways.\nconst { r, g, b, a } = horse.color\n// or\nconst { r, g, b, a } = horse.color.value\n// or\nconst { r, g, b, a } = horse.color.associations\n</code></pre>\n<p>When programmers think of enums, they inevitably think of\nthe switch/case construct. This works with Enumeration instances\nas well. That might look like this.</p>\n<pre class=\"prettyprint source lang-js\"><code>switch (horse.color.key) {\n  case Color.red.key: ...; break;\n  case Color.green.key: ...; break;\n  case Color.blue.key: ...; break;\n}\n</code></pre>\n<p>But this is largely because enums in many languages, outside of Swift,\nare often simple constant keys with values of either the same as their\nname or incrementing numbers. With Enumeration, each case is a fully\nfledged instance of the Enumeration type. So we could easily provide\nmethods that work on the aforementioned color channel value object.</p>\n<pre class=\"prettyprint source lang-js\"><code>class Color extends Enumeration {\n  toHex(includeAlpha = false) {\n    const { r, g, b, a } = this;\n    const base = `#` + [r, g, b]\n      .map(c => c.toString(16).padStart(2, '0')).join('');\n\n    if (includeAlpha)\n      return (base + a.toString(16));\n\n    return base;\n  }\n\n  static {\n    // define cases\n  }\n}\n</code></pre>\n<p>Now there is little need for the switch case. You can simply use it define\na hexadecimal color. The case becomes the controller, the value/associations\nbecome the model. The case knows how to operate on all types of this class,\nor in this case all colors. The value or associations object are the model\nand the methods such as toHex() belong to the controller.</p>\n<p>Given that we are using normal JavaScript to achieve this, everything is\nwhere you would expect it to be; <code>toHex()</code> lives on the <code>Color.prototype</code>\nobject and all normal JavaScript manipulation works here.</p>\n<pre class=\"prettyprint source lang-js\"><code>console.log(car.color.toHex())  // #ff0000\nconsole.log(horse.color.toHex())  // #c84c31\n</code></pre>\n<p>For more complex use cases you could use the Color.match() function</p>\n<pre class=\"prettyprint source lang-js\"><code>Color.match(horse.color, (color, enumCase, { r, g, b, a }) => {\n  // horse.color is Color.rgb with associated values\n  // enumCase is Color.rgb without associated values\n  // the third parameter is either an empty object or the custom\n  //   associations object that you can destructure expected content\n  //   from.\n\n  // do something with r, g, b, a if `horse.color` is a Color case type\n})\n</code></pre>","signature":null,"augments":[],"implements":[],"deprecated":null,"examples":[],"see":[],"since":null,"access":null,"readonly":false,"defaultvalue":null,"type":null,"members":{"classes":[],"methods":[{"slug":null,"name":"associate","kind":"function","title":"associate","source":{"file":"enumeration.mjs","line":268},"description":"<p>Creates a duplicate of this enumeration case, and assigns instance\nlevel associated key/value pairs on the copy. It is still of the\nsame enum class type, but has instance level associated value.</p>","signature":{"params":[{"name":"entries","type":null,"description":"","optional":false,"nullable":false,"variable":true,"defaultvalue":null}],"returns":[{"type":["*"],"description":"<p>an instance of this class</p>"}],"exceptions":[],"async":false,"generator":false},"augments":[],"implements":[],"deprecated":null,"examples":[],"see":[],"since":null,"access":null,"readonly":false,"defaultvalue":null,"type":null,"scope":"instance","memberof":"Enumeration","longname":"Enumeration#associate","members":{"classes":[],"methods":[],"staticMethods":[],"properties":[],"staticProperties":[],"events":[],"typedefs":[]}},{"slug":null,"name":"associated","kind":"function","title":"associated","source":{"file":"enumeration.mjs","line":306},"description":"<p>Shorthand for retrieving an internal associated value</p>","signature":{"params":[{"name":"key","type":["string","number","symbol"],"description":"<p>a key into the internal\nassociations store. Typically, this value is null.</p>","optional":false,"nullable":false,"variable":false,"defaultvalue":null}],"returns":[{"type":["any","null"],"description":"<p>null if there is no such named association\nor if there are no associations stored on this enum value.</p>"}],"exceptions":[],"async":false,"generator":false},"augments":[],"implements":[],"deprecated":null,"examples":[],"see":[],"since":null,"access":null,"readonly":false,"defaultvalue":null,"type":null,"scope":"instance","memberof":"Enumeration","longname":"Enumeration#associated","members":{"classes":[],"methods":[],"staticMethods":[],"properties":[],"staticProperties":[],"events":[],"typedefs":[]}},{"slug":null,"name":"is","kind":"function","title":"is","source":{"file":"enumeration.mjs","line":330},"description":"<p>Checks to see if this object is, or is loosely, the same as\nthe supplied <code>someCase</code> value. This is determined by comparing\nthe <code>.key</code> property.</p>","signature":{"params":[{"name":"someCase","type":["any"],"description":"<p>some object value that might have a\nmatching (double equals) key property value</p>","optional":false,"nullable":false,"variable":false,"defaultvalue":null}],"returns":[{"type":["boolean"],"description":"<p>true if the objects are loosely equal (==)\nor if each of <code>.key</code> values are loosely equal (==)</p>"}],"exceptions":[],"async":false,"generator":false},"augments":[],"implements":[],"deprecated":null,"examples":[],"see":[],"since":null,"access":null,"readonly":false,"defaultvalue":null,"type":null,"scope":"instance","memberof":"Enumeration","longname":"Enumeration#is","members":{"classes":[],"methods":[],"staticMethods":[],"properties":[],"staticProperties":[],"events":[],"typedefs":[]}},{"slug":null,"name":"toString","kind":"function","title":"toString","source":{"file":"enumeration.mjs","line":342},"description":"<p>Define the string representation of any given <a href=\"#Enumeration\">Enumeration</a>\ninstance to be its <code>.key</code> value.</p>","signature":{"params":[],"returns":[{"type":["string"],"description":"<p>the value of the <code>.key</code> property wrapped in\na call to <code>String()</code> to ensure conversion.</p>"}],"exceptions":[],"async":false,"generator":false},"augments":[],"implements":[],"deprecated":null,"examples":[],"see":[],"since":null,"access":null,"readonly":false,"defaultvalue":null,"type":null,"scope":"instance","memberof":"Enumeration","longname":"Enumeration#toString","members":{"classes":[],"methods":[],"staticMethods":[],"properties":[],"staticProperties":[],"events":[],"typedefs":[]}},{"slug":null,"name":"valueOf","kind":"function","title":"valueOf","source":{"file":"enumeration.mjs","line":376},"description":"<p>Define the result of a call to #valueOf to always be\nthe contents of the <code>.value</code> property.</p>","signature":{"params":[],"returns":[{"type":["any"],"description":"<p>the contents of the <code>.value</code> property</p>"}],"exceptions":[],"async":false,"generator":false},"augments":[],"implements":[],"deprecated":null,"examples":[],"see":[],"since":null,"access":null,"readonly":false,"defaultvalue":null,"type":null,"scope":"instance","memberof":"Enumeration","longname":"Enumeration#valueOf","members":{"classes":[],"methods":[],"staticMethods":[],"properties":[],"staticProperties":[],"events":[],"typedefs":[]}}],"staticMethods":[{"slug":null,"name":"toPrimitive","kind":"function","title":"toPrimitive","source":{"file":"enumeration.mjs","line":390},"description":"<p>Returns the <code>.key</code> value as a primitive, unless a conversion to\nnumber is requested. In which case, if the <code>.value</code> property is\nof type Number then it will be returned. In all other\ncases the result will be <code>String(this.key)</code>.</p>","signature":{"params":[],"returns":[{"type":["string","number","NaN"],"description":"<p>returns a String representation\nof the <code>.key</code> property unless a number is requested. See above\nfor custom logic pertaining to number coercion.</p>"}],"exceptions":[],"async":false,"generator":false},"augments":[],"implements":[],"deprecated":null,"examples":[],"see":[],"since":null,"access":null,"readonly":false,"defaultvalue":null,"type":null,"scope":"static","memberof":"Enumeration#Symbol","longname":"Enumeration#Symbol.toPrimitive","members":{"classes":[],"methods":[],"staticMethods":[],"properties":[],"staticProperties":[],"events":[],"typedefs":[]}},{"slug":null,"name":"iterator","kind":"function","title":"iterator","source":{"file":"enumeration.mjs","line":704},"description":"<p>Creates an iterator of all <a href=\"#Enumeration\">Enumeration</a> derived instances that\nare statically assigned to this class. Generally this is only useful\nif applied to child class of <code>Enumeration</code>.</p>","signature":{"params":[],"returns":[{"type":["Generator.<*, void, *>"],"description":"<p>an iterator that walks instances\nof derived <a href=\"#Enumeration\">Enumeration</a> classes and returns each key/value pair\nas arrays. <strong>This is the same as <code>Object.entries(ChildEnumerationClass)</code>\nand then filter the results for pairs whose values are instances of\n<code>ChildEnumerationClass</code></strong></p>"}],"exceptions":[],"async":false,"generator":true},"augments":[],"implements":[],"deprecated":null,"examples":[],"see":[],"since":null,"access":null,"readonly":false,"defaultvalue":null,"type":null,"scope":"static","memberof":"Enumeration.Symbol","longname":"Enumeration.Symbol.iterator","members":{"classes":[],"methods":[],"staticMethods":[],"properties":[],"staticProperties":[],"events":[],"typedefs":[]}},{"slug":null,"name":"asyncMatch","kind":"function","title":"asyncMatch","source":{"file":"enumeration.mjs","line":476},"description":"<p>Given an instance of a child class of <a href=\"#Enumeration\">Enumeration</a>, this code\nwalks through all valid enum cases for this class. If either present\nor missing are functions, they will be invoked and their results will\nbe what are returned from this match. If they are not functions, then\ntheir direct values will be returned. If they are not supplied, true\nis returned if the case is present, false is returned otherwise.</p>\n<p>In the case of present being a function, its signature is expected\nto be something like the following. Note that this will be awaited\nbefore returning its result.</p>\n<pre class=\"prettyprint source lang-js\"><code>async present(instance, baseCase, associations) => Promise&lt;any>\n</code></pre>\n<p>where</p>\n<ul>\n<li><strong>instance</strong> is the instance of the enum supplied</li>\n<li><strong>baseCase</strong> is the instance of the enum matching <code>instance</code></li>\n<li><strong>associations</strong> is either an empty object when there are no custom\nassociations, or the associations object itself.</li>\n</ul>\n<p>and missing simply receives the instance passed to <code>asyncMatch()</code>,\nbut if missing is an async function, its call will be awaited before\nreturning a result.</p>\n<pre class=\"prettyprint source lang-js\"><code>async missing(instance) => Promise&lt;any>\n</code></pre>\n<p>where</p>\n<ul>\n<li><strong>instance</strong> is the instance of the enum supplied</li>\n</ul>\n<p>Some examples:</p>\n<pre class=\"prettyprint source lang-js\"><code>Color.match(Color.red) // returns true\nColor.match(Color.red, (_,__,{r}) => r) // returns 255\nColor.match(null) // returns false\nColor.match(null, (_,__,{r}) => r, () => 0) // returns 0\nColor.match(null, 'red', 'missing') // returns 'missing'\n\nColor.match(horse.color) // returns true\nColor.match(horse.color, instance => instance.toHex()) // '#c84c31'\n</code></pre>","signature":{"params":[{"name":"instance","type":["Enumeration"],"description":"<p>an instance of any <a href=\"#Enumeration\">Enumeration</a>\nderived class. With or without associated values.</p>","optional":false,"nullable":false,"variable":false,"defaultvalue":null},{"name":"present","type":["function","any","undefined"],"description":"<p>an async function that computes a value or a variable value to\nreturn if the <code>instance</code> is one of the known <a href=\"#Enumeration\">Enumeration</a> cases\nfor this subclass. Defaults to <code>true</code> if not supplied</p>","optional":false,"nullable":false,"variable":false,"defaultvalue":null},{"name":"missing","type":["function"],"description":"<p>an async function\nthat computes a value or a variable value to return if the <code>instance</code> is\nnot one of the known cases for this subclass. Defaults to <code>false</code> if not\nsupplied.</p>","optional":false,"nullable":false,"variable":false,"defaultvalue":null}],"returns":[{"type":["any"],"description":"<p>the result of either <code>present</code>, or <code>missing</code>, using the\ndefault values of <code>true</code> and <code>false</code>, respectively, should they not be\nsupplied. The outcome is determined by the existence of the supplied\n<code>instance</code> object being a known case for this enum subclass.</p>"}],"exceptions":[],"async":true,"generator":false},"augments":[],"implements":[],"deprecated":null,"examples":[],"see":[],"since":null,"access":null,"readonly":false,"defaultvalue":null,"type":null,"scope":"static","memberof":"Enumeration","longname":"Enumeration.asyncMatch","members":{"classes":[],"methods":[],"staticMethods":[],"properties":[],"staticProperties":[],"events":[],"typedefs":[]}},{"slug":null,"name":"cases","kind":"function","title":"cases","source":{"file":"enumeration.mjs","line":595},"description":"<p>Creates an iterator of all <a href=\"#Enumeration\">Enumeration</a> derived instances that\nare statically assigned to this class. Generally this is only useful\nif applied to child class of <code>Enumeration</code>.</p>","signature":{"params":[],"returns":[{"type":["Generator.<string, void, *>"],"description":"<p>an iterator that walks instances\nof derived <a href=\"#Enumeration\">Enumeration</a> classes and returns their <code>.key</code> values</p>"}],"exceptions":[],"async":false,"generator":true},"augments":[],"implements":[],"deprecated":null,"examples":[],"see":[],"since":null,"access":null,"readonly":false,"defaultvalue":null,"type":null,"scope":"static","memberof":"Enumeration","longname":"Enumeration.cases","members":{"classes":[],"methods":[],"staticMethods":[],"properties":[],"staticProperties":[],"events":[],"typedefs":[]}},{"slug":null,"name":"define","kind":"function","title":"define","source":{"file":"enumeration.mjs","line":549},"description":"<p>Used when creating a static instance of <a href=\"#Enumeration\">Enumeration</a>. Generally\nthis is done as follows:</p>","signature":{"params":[{"name":"key","type":["string","number","symbol"],"description":"<p>the case name of the this particular\nenumeration instance.</p>","optional":false,"nullable":false,"variable":false,"defaultvalue":null},{"name":"value","type":null,"description":"","optional":false,"nullable":false,"variable":false,"defaultvalue":null},{"name":"customizeInstance","type":["function","object"],"description":"<p>defaults to\n<code>undefined</code>, but when it is passed in as a function, the signature\nwould be to take an instance of this Enumeration class and return\none (presumably after modification), or in the form of an object whose\nproperty descriptors are copied onto the defined instance. This later\napproach retains getter and setter application as well as other rare\ndescriptor modifications.</p>","optional":true,"nullable":false,"variable":false,"defaultvalue":null}],"returns":[{"type":["*"],"description":"<p>an instance of this <a href=\"#Enumeration\">Enumeration</a> class type.</p>"}],"exceptions":[],"async":false,"generator":false},"augments":[],"implements":[],"deprecated":null,"examples":["class Shape extends Enumeration {\n  static {\n    Shape.define('cylinder')\n    Shape.define('cube')\n    Shade.define('other')\n  }\n}"],"see":[],"since":null,"access":null,"readonly":false,"defaultvalue":null,"type":null,"scope":"static","memberof":"Enumeration","longname":"Enumeration.define","members":{"classes":[],"methods":[],"staticMethods":[],"properties":[],"staticProperties":[],"events":[],"typedefs":[]}},{"slug":null,"name":"match","kind":"function","title":"match","source":{"file":"enumeration.mjs","line":654},"description":"<p>Given an instance of a child class of <a href=\"#Enumeration\">Enumeration</a>, this code\nwalks through all valid enum cases for this class. If either present\nor missing are functions, they will be invoked and their results will\nbe what are returned from this match. If they are not functions, then\ntheir direct values will be returned. If they are not supplied, true\nis returned if the case is present, false is returned otherwise.</p>\n<p>In the case of present being a function, its signature is expected\nto be something like the following:</p>\n<pre class=\"prettyprint source lang-js\"><code>present(instance, baseCase, associations) => any\n</code></pre>\n<p>where</p>\n<ul>\n<li><strong>instance</strong> is the instance of the enum supplied</li>\n<li><strong>baseCase</strong> is the instance of the enum matching <code>instance</code></li>\n<li><strong>associations</strong> is either an empty object when there are no custom\nassociations, or the associations object itself.</li>\n</ul>\n<p>and missing simply receives the instance passed to <code>match()</code> as its\nonly parameter.</p>\n<pre class=\"prettyprint source lang-js\"><code>missing(instance) => any\n</code></pre>\n<p>where</p>\n<ul>\n<li><strong>instance</strong> is the instance of the enum supplied</li>\n</ul>\n<p>Some examples:</p>\n<pre class=\"prettyprint source lang-js\"><code>Color.match(Color.red) // returns true\nColor.match(Color.red, (_,__,{r}) => r) // returns 255\nColor.match(null) // returns false\nColor.match(null, (_,__,{r}) => r, () => 0) // returns 0\nColor.match(null, 'red', 'missing') // returns 'missing'\n\nColor.match(horse.color) // returns true\nColor.match(horse.color, instance => instance.toHex()) // '#c84c31'\n</code></pre>","signature":{"params":[{"name":"instance","type":["Enumeration"],"description":"<p>an instance of any <a href=\"#Enumeration\">Enumeration</a>\nderived class. With or without associated values.</p>","optional":false,"nullable":false,"variable":false,"defaultvalue":null},{"name":"present","type":["function","any","undefined"],"description":"<p>a function that computes a value or a variable value to\nreturn if the <code>instance</code> is one of the known <a href=\"#Enumeration\">Enumeration</a> cases\nfor this subclass. Defaults to <code>true</code> if not supplied</p>","optional":false,"nullable":false,"variable":false,"defaultvalue":null},{"name":"missing","type":["function"],"description":"<p>a function\nthat computes a value or a variable value to return if the <code>instance</code> is\nnot one of the known cases for this subclass. Defaults to <code>false</code> if not\nsupplied.</p>","optional":false,"nullable":false,"variable":false,"defaultvalue":null}],"returns":[{"type":["any"],"description":"<p>the result of either <code>present</code>, or <code>missing</code>, using the\ndefault values of <code>true</code> and <code>false</code>, respectively, should they not be\nsupplied. The outcome is determined by the existence of the supplied\n<code>instance</code> object being a known case for this enum subclass.</p>"}],"exceptions":[],"async":false,"generator":false},"augments":[],"implements":[],"deprecated":null,"examples":[],"see":[],"since":null,"access":null,"readonly":false,"defaultvalue":null,"type":null,"scope":"static","memberof":"Enumeration","longname":"Enumeration.match","members":{"classes":[],"methods":[],"staticMethods":[],"properties":[],"staticProperties":[],"events":[],"typedefs":[]}},{"slug":null,"name":"values","kind":"function","title":"values","source":{"file":"enumeration.mjs","line":689},"description":"<p>Creates an iterator of all <a href=\"#Enumeration\">Enumeration</a> derived instances that\nare statically assigned to this class. Generally this is only useful\nif applied to child class of <code>Enumeration</code>.</p>","signature":{"params":[],"returns":[{"type":["Generator.<string, void, *>"],"description":"<p>an iterator that walks instances\nof derived <a href=\"#Enumeration\">Enumeration</a> classes and returns their <code>.value</code> values</p>"}],"exceptions":[],"async":false,"generator":true},"augments":[],"implements":[],"deprecated":null,"examples":[],"see":[],"since":null,"access":null,"readonly":false,"defaultvalue":null,"type":null,"scope":"static","memberof":"Enumeration","longname":"Enumeration.values","members":{"classes":[],"methods":[],"staticMethods":[],"properties":[],"staticProperties":[],"events":[],"typedefs":[]}}],"properties":[{"slug":null,"name":"associations","kind":"property","title":"associations","source":{"file":"enumeration.mjs","line":195},"description":"<p>For <a href=\"#Enumeration\">Enumeration</a> instances that have instance level associated\nvalues. This is uncommon but is modeled after Swift's enums with\nassociated values. This object is <code>null</code> if there are no associations.</p>","signature":null,"augments":[],"implements":[],"deprecated":null,"examples":[],"see":[],"since":null,"access":null,"readonly":false,"defaultvalue":null,"type":["object"],"scope":"instance","memberof":"Enumeration","longname":"Enumeration#associations","members":{"classes":[],"methods":[],"staticMethods":[],"properties":[],"staticProperties":[],"events":[],"typedefs":[]}},{"slug":null,"name":"case","kind":"property","title":"case","source":{"file":"enumeration.mjs","line":366},"description":"<p>Returns a combination of the class' name followed by this\ninstances key value. This can be more explicit than just using\nthe <code>.key</code> property.</p>","signature":null,"augments":[],"implements":[],"deprecated":null,"examples":["class Shape extends Enumeration {\n  static {\n    Shape.define('circle')\n    Shape.define('square')\n  }\n}\n\nconsole.log(Shape.circle.case) // 'Shape.circle'\n\n// ['Shape.circle', 'Shape.square']\nconsole.log([...Shape.values()].map(s => s.case))"],"see":[],"since":null,"access":null,"readonly":false,"defaultvalue":null,"type":["string"],"scope":"instance","memberof":"Enumeration","longname":"Enumeration#case","members":{"classes":[],"methods":[],"staticMethods":[],"properties":[],"staticProperties":[],"events":[],"typedefs":[]}},{"slug":null,"name":"hasAssociatedValues","kind":"property","title":"hasAssociatedValues","source":{"file":"enumeration.mjs","line":316},"description":"<p>Returns true if there is an associated value for this enumeration case.</p>","signature":null,"augments":[],"implements":[],"deprecated":null,"examples":[],"see":[],"since":null,"access":null,"readonly":false,"defaultvalue":null,"type":null,"scope":"instance","memberof":"Enumeration","longname":"Enumeration#hasAssociatedValues","members":{"classes":[],"methods":[],"staticMethods":[],"properties":[],"staticProperties":[],"events":[],"typedefs":[]}},{"slug":null,"name":"key","kind":"property","title":"key","source":{"file":"enumeration.mjs","line":178},"description":"<p>The case name for this <a href=\"#Enumeration\">Enumeration</a> instance.</p>","signature":null,"augments":[],"implements":[],"deprecated":null,"examples":[],"see":[],"since":null,"access":null,"readonly":false,"defaultvalue":null,"type":["string","symbol"],"scope":"instance","memberof":"Enumeration","longname":"Enumeration#key","members":{"classes":[],"methods":[],"staticMethods":[],"properties":[],"staticProperties":[],"events":[],"typedefs":[]}},{"slug":null,"name":"value","kind":"property","title":"value","source":{"file":"enumeration.mjs","line":186},"description":"<p>The value for this case name, defaults to the same as the\ncase name unless specifically supplied.</p>","signature":null,"augments":[],"implements":[],"deprecated":null,"examples":[],"see":[],"since":null,"access":null,"readonly":false,"defaultvalue":null,"type":["any"],"scope":"instance","memberof":"Enumeration","longname":"Enumeration#value","members":{"classes":[],"methods":[],"staticMethods":[],"properties":[],"staticProperties":[],"events":[],"typedefs":[]}}],"staticProperties":[{"slug":null,"name":"toStringTag","kind":"property","title":"toStringTag","source":{"file":"enumeration.mjs","line":413},"description":"<p>Generates a custom tag name that matches this instances class name.</p>","signature":null,"augments":[],"implements":[],"deprecated":null,"examples":["class Shape extends Enumeration {\n  static { Shape.define('circle') }\n}\n\nconsole.log(Shape.circle[Symbol.toStringTag]) // 'Shape'"],"see":[],"since":null,"access":null,"readonly":false,"defaultvalue":null,"type":null,"scope":"static","memberof":"Enumeration#Symbol","longname":"Enumeration#Symbol.toStringTag","members":{"classes":[],"methods":[],"staticMethods":[],"properties":[],"staticProperties":[],"events":[],"typedefs":[]}}],"events":[],"typedefs":[]}},"is":{"slug":"is","name":"is","kind":"object","title":"is","source":{"file":"enumeration.mjs","line":7},"description":"<p>Tiny internal toolkit to work with types at runtime when dealing\nwith Enumerations.</p>","signature":null,"augments":[],"implements":[],"deprecated":null,"examples":[],"see":[],"since":null,"access":null,"readonly":false,"defaultvalue":null,"type":["TypeToolbox"],"members":{"classes":[],"methods":[{"slug":null,"name":"object","kind":"function","title":"object","source":{"file":"enumeration.mjs","line":8},"description":"","signature":{"params":[],"returns":[],"exceptions":[],"async":false,"generator":false},"augments":[],"implements":[],"deprecated":null,"examples":[],"see":[],"since":null,"access":null,"readonly":false,"defaultvalue":null,"type":null,"scope":"instance","memberof":null,"longname":"is.object","members":{"classes":[],"methods":[],"staticMethods":[],"properties":[],"staticProperties":[],"events":[],"typedefs":[]}},{"slug":null,"name":"function","kind":"function","title":"function","source":{"file":"enumeration.mjs","line":9},"description":"","signature":{"params":[],"returns":[],"exceptions":[],"async":false,"generator":false},"augments":[],"implements":[],"deprecated":null,"examples":[],"see":[],"since":null,"access":null,"readonly":false,"defaultvalue":null,"type":null,"scope":"instance","memberof":null,"longname":"is.function","members":{"classes":[],"methods":[],"staticMethods":[],"properties":[],"staticProperties":[],"events":[],"typedefs":[]}},{"slug":null,"name":"array","kind":"function","title":"array","source":{"file":"enumeration.mjs","line":10},"description":"","signature":{"params":[],"returns":[],"exceptions":[],"async":false,"generator":false},"augments":[],"implements":[],"deprecated":null,"examples":[],"see":[],"since":null,"access":null,"readonly":false,"defaultvalue":null,"type":null,"scope":"instance","memberof":null,"longname":"is.array","members":{"classes":[],"methods":[],"staticMethods":[],"properties":[],"staticProperties":[],"events":[],"typedefs":[]}},{"slug":null,"name":"number","kind":"function","title":"number","source":{"file":"enumeration.mjs","line":11},"description":"","signature":{"params":[],"returns":[],"exceptions":[],"async":false,"generator":false},"augments":[],"implements":[],"deprecated":null,"examples":[],"see":[],"since":null,"access":null,"readonly":false,"defaultvalue":null,"type":null,"scope":"instance","memberof":null,"longname":"is.number","members":{"classes":[],"methods":[],"staticMethods":[],"properties":[],"staticProperties":[],"events":[],"typedefs":[]}},{"slug":null,"name":"objectEntry","kind":"function","title":"objectEntry","source":{"file":"enumeration.mjs","line":12},"description":"","signature":{"params":[],"returns":[],"exceptions":[],"async":false,"generator":false},"augments":[],"implements":[],"deprecated":null,"examples":[],"see":[],"since":null,"access":null,"readonly":false,"defaultvalue":null,"type":null,"scope":"instance","memberof":null,"longname":"is.objectEntry","members":{"classes":[],"methods":[],"staticMethods":[],"properties":[],"staticProperties":[],"events":[],"typedefs":[]}},{"slug":null,"name":"objectKey","kind":"function","title":"objectKey","source":{"file":"enumeration.mjs","line":15},"description":"","signature":{"params":[],"returns":[],"exceptions":[],"async":false,"generator":false},"augments":[],"implements":[],"deprecated":null,"examples":[],"see":[],"since":null,"access":null,"readonly":false,"defaultvalue":null,"type":null,"scope":"instance","memberof":null,"longname":"is.objectKey","members":{"classes":[],"methods":[],"staticMethods":[],"properties":[],"staticProperties":[],"events":[],"typedefs":[]}}],"staticMethods":[],"properties":[],"staticProperties":[],"events":[],"typedefs":[]}},"SubscriptProxy":{"slug":"SubscriptProxy","name":"SubscriptProxy","kind":"function","title":"SubscriptProxy","source":{"file":"enumeration.mjs","line":728},"description":"<p>Creates a prototype safe Proxy object, specific to\n<a href=\"#Enumeration\">Enumeration</a> instances, that if a getter tries to reach a named\nproperty that doesn't exist on the enum, <strong>and</strong> <code>.hasAssociatedValues</code>\nis <code>true</code>, then it will attempt to return <code>this.associations[property]</code>\ninstead.</p>","signature":{"params":[{"name":"proxied","type":["object"],"description":"<p>the object to the behavior to; typically this is\nthe prototype of the class itself. See the constructor in Enumeration for\nusage.</p>","optional":false,"nullable":false,"variable":false,"defaultvalue":null}],"returns":[{"type":["*","null","object"],"description":"<p>a Proxy instance around the supplied\nobject to be proxied with the above defined behavior.</p>"}],"exceptions":[],"async":false,"generator":false},"augments":[],"implements":[],"deprecated":null,"examples":[],"see":[],"since":null,"access":null,"readonly":false,"defaultvalue":null,"type":null,"members":{"classes":[],"methods":[],"staticMethods":[],"properties":[],"staticProperties":[],"events":[],"typedefs":[]}},"TypeToolbox":{"slug":"TypeToolbox","name":"TypeToolbox","kind":"typedef","title":"TypeToolbox","source":{"file":"enumeration.mjs","line":771},"description":"","signature":null,"augments":[],"implements":[],"deprecated":null,"examples":[],"see":[],"since":null,"access":null,"readonly":false,"defaultvalue":null,"type":["object"],"members":{"classes":[],"methods":[],"staticMethods":[],"properties":[],"staticProperties":[],"events":[],"typedefs":[]}}},"readme":"<h1>@nejs/enumeration</h1>\n<p>A zero-dependency ESM library that brings Swift-style runtime enumerations to JavaScript. Unlike TypeScript's <code>enum</code>, which only exists at compile time, <code>Enumeration</code> instances are real objects you can pass around, extend with methods, pattern-match against, and attach per-instance associated values to.</p>\n<h2>Installation</h2>\n<pre class=\"prettyprint source lang-sh\"><code>npm install @nejs/enumeration\n</code></pre>\n<h2>Quick Start</h2>\n<pre class=\"prettyprint source lang-js\"><code>import Enumeration from '@nejs/enumeration'\n\nclass Direction extends Enumeration {\n  static {\n    Direction.define('north')\n    Direction.define('south')\n    Direction.define('east')\n    Direction.define('west')\n  }\n}\n\nconsole.log(Direction.north.key)   // 'north'\nconsole.log(Direction.north.value) // 'north'  (defaults to key when no value given)\nconsole.log(`${Direction.north}`)  // 'north'  (toString returns key)\nconsole.log(Direction.north.case)  // 'Direction.north'\n</code></pre>\n<h2>Defining Cases</h2>\n<p><code>ClassName.define(key, value?, customizeInstance?)</code> registers a static getter on the class for each case. The call must live inside a <code>static {}</code> initializer block.</p>\n<pre class=\"prettyprint source lang-js\"><code>class Status extends Enumeration {\n  static {\n    Status.define('ok',    200)\n    Status.define('notFound', 404)\n    Status.define('error', 500)\n  }\n}\n\nStatus.ok.key    // 'ok'\nStatus.ok.value  // 200\n+Status.ok       // 200  (valueOf returns .value; numeric coercion uses .value if it is a number)\n</code></pre>\n<h3>Object values and the SubscriptProxy</h3>\n<p>When a case's value is an object, its properties are accessible directly on the enum instance via the built-in <code>SubscriptProxy</code>:</p>\n<pre class=\"prettyprint source lang-js\"><code>class Color extends Enumeration {\n  static {\n    Color.define('red',   { r: 255, g: 0,   b: 0,   a: 255 })\n    Color.define('green', { r: 0,   g: 255, b: 0,   a: 255 })\n    Color.define('blue',  { r: 0,   g: 0,   b: 255, a: 255 })\n  }\n}\n\nColor.red.value.r  // 255 — via .value\nColor.red.r        // 255 — via SubscriptProxy shorthand\nconst { r, g, b, a } = Color.red  // destructuring works too\n</code></pre>\n<p>The proxy lookup order for a missing property is:</p>\n<ol>\n<li><code>this.associations[property]</code> (if associated values are present)</li>\n<li><code>this.value[property]</code> (if <code>.value</code> is an object)</li>\n<li>Normal prototype chain</li>\n</ol>\n<h3>Customizing instances</h3>\n<p><code>define()</code> accepts an optional third argument to customize the new case instance. Pass an object with property descriptors (getters, setters, etc.) or a function <code>(instance) =&gt; instance</code>:</p>\n<pre class=\"prettyprint source lang-js\"><code>class Color extends Enumeration {\n  static {\n    // Custom getter that redirects .value to .associations\n    Color.define('rgb', null, {\n      get value() { return this.associations ?? { r: 0, g: 0, b: 0, a: 255 } }\n    })\n  }\n}\n</code></pre>\n<h2>Associated Values</h2>\n<p><code>associate(...entries)</code> creates a variant copy of an enum case with per-instance data attached — modeled after Swift's enums with associated values. The original static case is never mutated.</p>\n<pre class=\"prettyprint source lang-js\"><code>const chestnut = Color.rgb.associate({ r: 200, g: 76, b: 49, a: 255 })\n\nchestnut.key              // 'rgb'  — still the same case type\nchestnut instanceof Color // true\nchestnut.hasAssociatedValues // true\nchestnut.r                // 200   — via SubscriptProxy -> associations\nchestnut.associations     // { r: 200, g: 76, b: 49, a: 255 }\nchestnut.associated('r')  // 200   — explicit lookup, safe when property names collide\n</code></pre>\n<p><code>associate()</code> accepts entries in three forms, and multiple entries can be passed:</p>\n<pre class=\"prettyprint source lang-js\"><code>// Object — key/value pairs merged in\ninstance.associate({ r: 255, g: 0, b: 0 })\n\n// Object entry (2-element array)\ninstance.associate(['r', 255])\n\n// Bare key — value equals the key itself\ninstance.associate('label')  // associations.label === 'label'\n</code></pre>\n<p>If the instance already has associated values, <code>associate()</code> merges into the existing associations and returns <code>this</code> instead of creating a new copy.</p>\n<h2>Adding Methods</h2>\n<p>Because each enum case is a real instance of the class, instance methods live on the prototype and work across all cases:</p>\n<pre class=\"prettyprint source lang-js\"><code>class Color extends Enumeration {\n  toHex(includeAlpha = false) {\n    const { r, g, b, a } = this\n    const base = '#' + [r, g, b].map(c => c.toString(16).padStart(2, '0')).join('')\n    return includeAlpha ? base + a.toString(16) : base\n  }\n\n  static {\n    Color.define('red',   { r: 255, g: 0, b: 0, a: 255 })\n    Color.define('green', { r: 0, g: 255, b: 0, a: 255 })\n  }\n}\n\nColor.red.toHex()         // '#ff0000'\n\nconst chestnut = Color.rgb.associate({ r: 200, g: 76, b: 49, a: 255 })\nchestnut.toHex()          // '#c84c31'\n</code></pre>\n<h2>Pattern Matching</h2>\n<h3><code>ClassName.match(instance, present?, missing?)</code></h3>\n<p>Synchronous pattern match. Checks whether <code>instance</code> is a known case of the enum, then calls (or returns) <code>present</code> or <code>missing</code>:</p>\n<pre class=\"prettyprint source lang-js\"><code>// Boolean shorthand\nColor.match(Color.red)         // true\nColor.match(null)              // false\nColor.match('anything')        // false\n\n// third arg is .value when it is an object and there are no associations\nColor.match(Color.red, (_, __, { r }) => r)        // 255\n\n// third arg is .associations when the instance has associated values\nconst chestnut = Color.rgb.associate({ r: 200, g: 76, b: 49, a: 255 })\nColor.match(chestnut, (_, __, { r }) => r)         // 200\n\n// third arg is {} when value is not an object and there are no associations\n// Direction.north.value === 'north' (a string)\nDirection.match(Direction.north, (_, __, obj) => obj)  // {}\n\n// With plain values instead of functions\nColor.match(null, 'found', 'not found')            // 'not found'\n\n// missing handler receives the original instance\nColor.match(null, (_, __, { r }) => r, () => 0)    // 0\n\n// instance methods are usable directly in the handler\nColor.match(chestnut, c => c.toHex())              // '#c84c31'\n</code></pre>\n<p>The <code>present</code> callback signature: <code>(instance, baseCase, associations) =&gt; any</code></p>\n<ul>\n<li><code>instance</code> — the value passed in (may have custom associated values)</li>\n<li><code>baseCase</code> — the matched static case (no associated values)</li>\n<li><code>associations</code> — resolved in priority order: <code>instance.associations</code> if set, then <code>instance.value</code> if it is an object, otherwise <code>{}</code></li>\n</ul>\n<p>The <code>missing</code> callback signature: <code>(instance) =&gt; any</code></p>\n<h3><code>ClassName.asyncMatch(instance, present?, missing?)</code></h3>\n<p>Identical API, but both callbacks may be <code>async</code> and the result is always a <code>Promise</code>.</p>\n<h3><code>instance.is(someCase)</code> / <code>ClassName.is(left, right)</code></h3>\n<p>Loose equality check on <code>.key</code> values:</p>\n<pre class=\"prettyprint source lang-js\"><code>Color.red.is(Color.red)                  // true\nColor.is(chestnut, Color.rgb)            // true  (same key, different associations)\n</code></pre>\n<h2>Iterating Cases</h2>\n<pre class=\"prettyprint source lang-js\"><code>// All [key, value] pairs\nfor (const [key, value] of Color) { ... }\n\n// Just keys\nfor (const key of Color.cases()) { ... }\n\n// Just values (Enumeration instances)\nfor (const value of Color.values()) { ... }\n\n// Array helpers\n[...Color.cases()]   // ['red', 'green', 'blue', 'rgb']\n[...Color.values()]  // [Color.red, Color.green, Color.blue, Color.rgb]\n\n// .case getter for fully-qualified name\n[...Color.values()].map(c => c.case)\n// ['Color.red', 'Color.green', 'Color.blue', 'Color.rgb']\n</code></pre>\n<p><code>Symbol.iterator</code> only yields entries whose values are <code>instanceof</code> the class, so inherited static properties from <code>Enumeration</code> itself are excluded.</p>\n<h2>Coercion and Serialization</h2>\n<table>\n<thead>\n<tr>\n<th>Operation</th>\n<th>Result</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>String(Color.red)</code></td>\n<td><code>'red'</code> (key)</td>\n</tr>\n<tr>\n<td><code>`${Color.red}`</code></td>\n<td><code>'red'</code> (key)</td>\n</tr>\n<tr>\n<td><code>Color.red.valueOf()</code></td>\n<td><code>{ r: 255, g: 0, b: 0, a: 255 }</code> (value)</td>\n</tr>\n<tr>\n<td><code>+Status.ok</code></td>\n<td><code>200</code> (numeric hint returns <code>.value</code> when it is a number)</td>\n</tr>\n<tr>\n<td><code>Object.prototype.toString.call(Color.red)</code></td>\n<td><code>'[object Color]'</code> (<code>Symbol.toStringTag</code>)</td>\n</tr>\n</tbody>\n</table>\n<h2>API Reference</h2>\n<h3><code>Enumeration</code> instance properties</h3>\n<table>\n<thead>\n<tr>\n<th>Property</th>\n<th>Type</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>.key</code></td>\n<td><code>string | number | symbol</code></td>\n<td>Case name</td>\n</tr>\n<tr>\n<td><code>.value</code></td>\n<td><code>any</code></td>\n<td>Case value; defaults to <code>.key</code> if not supplied</td>\n</tr>\n<tr>\n<td><code>.associations</code></td>\n<td><code>object | null</code></td>\n<td>Per-instance associated data; <code>null</code> unless <code>.associate()</code> was called</td>\n</tr>\n<tr>\n<td><code>.hasAssociatedValues</code></td>\n<td><code>boolean</code></td>\n<td><code>true</code> when <code>.associations !== null</code></td>\n</tr>\n<tr>\n<td><code>.case</code></td>\n<td><code>string</code></td>\n<td><code>'ClassName.key'</code></td>\n</tr>\n</tbody>\n</table>\n<h3><code>Enumeration</code> instance methods</h3>\n<table>\n<thead>\n<tr>\n<th>Method</th>\n<th>Returns</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>.associate(...entries)</code></td>\n<td><code>this | new instance</code></td>\n<td>Attach associated values</td>\n</tr>\n<tr>\n<td><code>.associated(key)</code></td>\n<td><code>any | null</code></td>\n<td>Read one associated value by key</td>\n</tr>\n<tr>\n<td><code>.is(someCase)</code></td>\n<td><code>boolean</code></td>\n<td>Loose key equality check</td>\n</tr>\n<tr>\n<td><code>.toString()</code></td>\n<td><code>string</code></td>\n<td><code>String(this.key)</code></td>\n</tr>\n<tr>\n<td><code>.valueOf()</code></td>\n<td><code>any</code></td>\n<td><code>this.value</code></td>\n</tr>\n</tbody>\n</table>\n<h3><code>Enumeration</code> static methods</h3>\n<table>\n<thead>\n<tr>\n<th>Method</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>define(key, value?, customizeInstance?)</code></td>\n<td>Register a new enum case</td>\n</tr>\n<tr>\n<td><code>match(instance, present?, missing?)</code></td>\n<td>Synchronous pattern match</td>\n</tr>\n<tr>\n<td><code>asyncMatch(instance, present?, missing?)</code></td>\n<td>Async pattern match</td>\n</tr>\n<tr>\n<td><code>is(left, right)</code></td>\n<td>Loose key equality check</td>\n</tr>\n<tr>\n<td><code>*[Symbol.iterator]()</code></td>\n<td>Yields <code>[key, value]</code> pairs</td>\n</tr>\n<tr>\n<td><code>*cases()</code></td>\n<td>Yields case keys</td>\n</tr>\n<tr>\n<td><code>*values()</code></td>\n<td>Yields case instances</td>\n</tr>\n</tbody>\n</table>\n<h2>License</h2>\n<p>MIT</p>","sources":{"enumeration.mjs":"<span class=\"hljs-comment\">/**\n * Tiny internal toolkit to work with types at runtime when dealing\n * with Enumerations.\n *\n * <span class=\"hljs-doctag\">@type</span> {TypeToolbox}\n */</span>\nexport <span class=\"hljs-keyword\">const</span> <span class=\"hljs-keyword\">is</span> = {\n  <span class=\"hljs-keyword\">object</span>(v) { <span class=\"hljs-keyword\">return</span> !!(v &amp;&amp; typeof (v) === <span class=\"hljs-string\">&#x27;object&#x27;</span>) },\n  function(v) { <span class=\"hljs-keyword\">return</span> (typeof(v) === <span class=\"hljs-string\">&#x27;function&#x27;</span>) || v instanceof Function },\n  array(v) { <span class=\"hljs-keyword\">return</span> Array.isArray(v) },\n  number(v) { <span class=\"hljs-keyword\">return</span> (typeof(v) === <span class=\"hljs-string\">&#x27;number&#x27;</span> || v instanceof Number)},\n  objectEntry(v) {\n    <span class=\"hljs-keyword\">return</span> <span class=\"hljs-keyword\">this</span>.array(v) &amp;&amp; v.length === <span class=\"hljs-number\">2</span> &amp;&amp; <span class=\"hljs-keyword\">this</span>.objectKey(v[<span class=\"hljs-number\">0</span>])\n  },\n  objectKey(v) {\n    <span class=\"hljs-keyword\">const</span> types = [<span class=\"hljs-string\">&#x27;symbol&#x27;</span>, <span class=\"hljs-string\">&#x27;string&#x27;</span>, <span class=\"hljs-string\">&#x27;number&#x27;</span>]\n\n    <span class=\"hljs-keyword\">return</span> types.some(type =&gt; typeof v === type)\n  },\n}\n\n<span class=\"hljs-comment\">/**\n * Allows the creation of static, complex, enumeration values\n * that can be used and identified at runtime (as opposed to\n * TypeScript enum values which apply only during compile time).\n *\n * Below is a simple example defining some colors on a `Color`\n * enum. Simple enums are a combination of key and value. In\n * the following case the key is the color name and the value\n * is an object with color channel data (red, green, blue, and\n * alpha channel values from 0 to 255).\n *\n * ```js\n * class Color extends Enumeration {\n *   static {\n *     Color.define(&#x27;red&#x27;, {r: 255, g: 0, b: 0, a: 255})\n *     Color.define(&#x27;green&#x27;, {r: 0, g:255, b: 0, a: 255})\n *     Color.define(&#x27;blue&#x27;, {r: 0, g: 0, b: 255, a: 255})\n *   }\n * }\n *\n * Color.red.value.r === 255 // true\n * Color.red.r === 255 // true; this uses the SubscriptProxy\n *\n * // Grab all four color channels&#x27; data in one go. This also\n * // uses the SubscriptProxy for syntactic sugar.\n * const { r, g, b, a } = Color.red\n * ```\n *\n * New types could be defined later with subsequent Color.define()\n * calls. But take the case of customizable color case such as\n * one that is different everytime it is used. Each instance of a\n * child class of an Enumeration has the option to associate values\n * to it.\n *\n * ```js\n * class Color extends Enumeration {\n *   static {\n *     // assume cases from above as well\n *\n *     // define an `rgb` case that assumes associated color\n *     // channel data.\n *     Color.define(&#x27;rgb&#x27;, {r: 0, g: 0, b: 0, a: 255})\n *   }\n * }\n *\n * let car = { model: &quot;Ferrari&quot;, color: Color.red }\n * let horse = {\n *   breed: &quot;American Saddlebred&quot;,\n *   color: Color.rgb.associate({ r: 200, g: 76, b: 49, a: 255 })\n * }\n * ```\n *\n * This last snippet of code creates a contrived horse object with\n * a chestnut/sorrel colored color. The enum case type for this is\n * still `Color.rgb`, but a rgb cased enum indicates at least three\n * color channels. It&#x27;s specific values are available through\n * the SubscriptProxy&#x27;s direct key value access, .r/.g/.b/.a, but\n * is stored not in the value but in the associations object. If we\n * wanted `Color.rgb` to directly surface its associated values, a\n * little more customization would be needed.\n *\n * ```js\n * class Color extends Enumeration {\n *   static {\n *     // assume cases from above as well\n *\n *     // define an `rgb` case that assumes associated color\n *     // channel data.\n *     Color.define(&#x27;rgb&#x27;, {r: 0, g: 0, b: 0, a: 255}, {\n *       // always return the associations object instead of the\n *       // value for an unmodified Color enum case.\n *       get value() { return this.associations }\n *     })\n *   }\n * }\n *\n * // Now, you can access it in all three ways.\n * const { r, g, b, a } = horse.color\n * // or\n * const { r, g, b, a } = horse.color.value\n * // or\n * const { r, g, b, a } = horse.color.associations\n * ```\n *\n * When programmers think of enums, they inevitably think of\n * the switch/case construct. This works with Enumeration instances\n * as well. That might look like this.\n *\n * ```js\n * switch (horse.color.key) {\n *   case Color.red.key: ...; break;\n *   case Color.green.key: ...; break;\n *   case Color.blue.key: ...; break;\n * }\n * ```\n *\n * But this is largely because enums in many languages, outside of Swift,\n * are often simple constant keys with values of either the same as their\n * name or incrementing numbers. With Enumeration, each case is a fully\n * fledged instance of the Enumeration type. So we could easily provide\n * methods that work on the aforementioned color channel value object.\n *\n * ```js\n * class Color extends Enumeration {\n *   toHex(includeAlpha = false) {\n *     const { r, g, b, a } = this;\n *     const base = `#` + [r, g, b]\n *       .map(c =&gt; c.toString(16).padStart(2, &#x27;0&#x27;)).join(&#x27;&#x27;);\n *\n *     if (includeAlpha)\n *       return (base + a.toString(16));\n *\n *     return base;\n *   }\n *\n *   static {\n *     // define cases\n *   }\n * }\n * ```\n *\n * Now there is little need for the switch case. You can simply use it define\n * a hexadecimal color. The case becomes the controller, the value/associations\n * become the model. The case knows how to operate on all types of this class,\n * or in this case all colors. The value or associations object are the model\n * and the methods such as toHex() belong to the controller.\n *\n * Given that we are using normal JavaScript to achieve this, everything is\n * where you would expect it to be; `toHex()` lives on the `Color.prototype`\n * object and all normal JavaScript manipulation works here.\n *\n * ```js\n * console.log(car.color.toHex())  // #ff0000\n * console.log(horse.color.toHex())  // #c84c31\n * ```\n *\n * For more complex use cases you could use the Color.match() function\n *\n * ```js\n * Color.match(horse.color, (color, enumCase, { r, g, b, a }) =&gt; {\n *   // horse.color is Color.rgb with associated values\n *   // enumCase is Color.rgb without associated values\n *   // the third parameter is either an empty object or the custom\n *   //   associations object that you can destructure expected content\n *   //   from.\n *\n *   // do something with r, g, b, a if `horse.color` is a Color case type\n * })\n * ```\n */</span>\nexport <span class=\"hljs-keyword\">class</span> <span class=\"hljs-title class_\">Enumeration</span> {\n  <span class=\"hljs-comment\">/**\n   * The case name for this {<span class=\"hljs-doctag\">@link</span> Enumeration} instance.\n   *\n   * <span class=\"hljs-doctag\">@type</span> {string|symbol}\n   */</span>\n  key;\n\n  <span class=\"hljs-comment\">/**\n   * The value for this case name, defaults to the same as the\n   * case name unless specifically supplied.\n   *\n   * <span class=\"hljs-doctag\">@type</span> {any}\n   */</span>\n  value;\n\n  <span class=\"hljs-comment\">/**\n   * For {<span class=\"hljs-doctag\">@link</span> Enumeration} instances that have instance level associated\n   * values. This is uncommon but is modeled after Swift&#x27;s enums with\n   * associated values. This object is `null` if there are no associations.\n   *\n   * <span class=\"hljs-doctag\">@type</span> {object}\n   */</span>\n  associations;\n\n  <span class=\"hljs-comment\">/**\n   * Creates a new simple {<span class=\"hljs-doctag\">@link</span> Enumeration} case with a key (case name)\n   * and associated value of any type. If no value is supplied, it will be\n   * set to the value of the key unless `acceptUndefinedValue` is set to\n   * true.\n   *\n   * <span class=\"hljs-doctag\">@param</span> {string|number|symbol} key the case name represented by this\n   * instance of {<span class=\"hljs-doctag\">@link</span> Enumeration}.\n   * <span class=\"hljs-doctag\">@param</span> {any} value any value for this enumeration case. If this is\n   * `undefined` and `acceptUndefinedValue` is set to false (the default)\n   * then the value will be identical to the `key`.\n   * <span class=\"hljs-doctag\">@param</span> {boolean} [acceptUndefinedValue=false] a flag that allows the\n   * rare case of setting a case&#x27;s value explicitly to `undefined`\n   * <span class=\"hljs-doctag\">@returns</span> {Enumeration} a new {<span class=\"hljs-doctag\">@link</span> Enumeration} value, or instance of\n   * whatever child class has extended `Enumeration`.\n   */</span>\n  <span class=\"hljs-keyword\">constructor</span>(key, value = undefined, acceptUndefinedValue = <span class=\"hljs-literal\">false</span>) {\n    <span class=\"hljs-keyword\">if</span> (value === undefined &amp;&amp; !acceptUndefinedValue) value = key;\n\n    Object.assign(<span class=\"hljs-keyword\">this</span>, { key, value });\n    Object.defineProperty(<span class=\"hljs-keyword\">this</span>, <span class=\"hljs-string\">&#x27;associations&#x27;</span>, {\n      value: <span class=\"hljs-literal\">null</span>,\n      configurable: <span class=\"hljs-literal\">true</span>,\n      enumerable: <span class=\"hljs-literal\">false</span>,\n      writable: <span class=\"hljs-literal\">true</span>,\n    });\n\n    <span class=\"hljs-comment\">/**\n     * Enables a subscript proxy on the instances of each {<span class=\"hljs-doctag\">@link</span> Enumeration}\n     * derived instance such that if there is no actual relevant property of\n     * the same name, and the instance has associated values, then it returns\n     * the value of the property of the same name on the associations object\n     * within.\n     *\n     * <span class=\"hljs-doctag\">@example</span>\n     * class Shapes extends Enumeration {\n     *   static {\n     *     Shapes.define(&#x27;other&#x27;)\n     *   }\n     * }\n     *\n     * let customShape = Shapes.other.associate({name: &#x27;Dodecahedron&#x27;})\n     *\n     * console.log(customShape.name) // &quot;Dodecahedron&quot;\n     *\n     * <span class=\"hljs-doctag\">@note</span> if there is an existing property of the same name, such as the\n     * `.key` or `.value`, it is safer to use the {<span class=\"hljs-doctag\">@link</span> Enumeration#associated}\n     * function retrieve the value. You have been warned.\n     */</span>\n    Object.setPrototypeOf(\n      <span class=\"hljs-keyword\">this</span>.<span class=\"hljs-keyword\">constructor</span>.prototype,\n      SubscriptProxy(\n        Object.create(Object.getPrototypeOf(<span class=\"hljs-keyword\">this</span>.<span class=\"hljs-keyword\">constructor</span>.prototype))\n      )\n    );\n\n    <span class=\"hljs-keyword\">return</span> <span class=\"hljs-keyword\">this</span>;\n  }\n\n  <span class=\"hljs-comment\">/**\n   * Creates a duplicate of this enumeration case, and assigns instance\n   * level associated key/value pairs on the copy. It is still of the\n   * same enum class type, but has instance level associated value.\n   *\n   * <span class=\"hljs-doctag\">@param</span> {...(object|string|number|symbol|[string|number|symbol,any])} entries\n   * a variadic list of objects (whose key/value pairs will be flattened\n   * and added to the associations), a key (string|number|symbol) whose\n   * value will be the same as the key, or an Object entry (i.e. an array with\n   * the first value being the key and the second value being the value).\n   * <span class=\"hljs-doctag\">@returns</span> {*} an instance of this class\n   */</span>\n  associate(...entries) {\n    <span class=\"hljs-keyword\">const</span> associations = { };\n\n    <span class=\"hljs-keyword\">for</span> (<span class=\"hljs-keyword\">const</span> entry of entries) {\n      <span class=\"hljs-keyword\">if</span> (<span class=\"hljs-keyword\">is</span>.objectEntry(entry)) {\n        associations[entry[<span class=\"hljs-number\">0</span>]] = entry[<span class=\"hljs-number\">1</span>];\n      }\n      <span class=\"hljs-keyword\">else</span> <span class=\"hljs-keyword\">if</span> (<span class=\"hljs-keyword\">is</span>.<span class=\"hljs-keyword\">object</span>(entry)) {\n        Object.assign(associations, entry);\n      }\n      <span class=\"hljs-keyword\">else</span> <span class=\"hljs-keyword\">if</span> (<span class=\"hljs-keyword\">is</span>.objectKey(entry)) {\n        associations[entry] = entry;\n      }\n    }\n\n    <span class=\"hljs-keyword\">if</span> (<span class=\"hljs-keyword\">this</span>.hasAssociatedValues) {\n      Object.assign(<span class=\"hljs-keyword\">this</span>.associations, associations);\n\n      <span class=\"hljs-keyword\">return</span> <span class=\"hljs-keyword\">this</span>;\n    }\n\n    <span class=\"hljs-comment\">// Create a duplicate of this case instance</span>\n    <span class=\"hljs-keyword\">const</span> variantCase = Object.create(<span class=\"hljs-keyword\">this</span>);\n\n    <span class=\"hljs-comment\">// Assign the associations object we created</span>\n    variantCase.associations = associations;\n\n    <span class=\"hljs-keyword\">return</span> variantCase;\n  }\n\n  <span class=\"hljs-comment\">/**\n   * Shorthand for retrieving an internal associated value\n   *\n   * <span class=\"hljs-doctag\">@param</span> {string|number|symbol} key a key into the internal\n   * associations store. Typically, this value is null.\n   * <span class=\"hljs-doctag\">@returns</span> {any|null} null if there is no such named association\n   * or if there are no associations stored on this enum value.\n   */</span>\n  associated(key) {\n    <span class=\"hljs-keyword\">return</span> <span class=\"hljs-keyword\">this</span>.associations?.[key];\n  }\n\n  <span class=\"hljs-comment\">/**\n   * Returns true if there is an associated value for this enumeration case.\n   *\n   * <span class=\"hljs-doctag\">@returns</span> {boolean} true if associations exist, denoting this is as\n   * a variant case; false otherwise.\n   */</span>\n  <span class=\"hljs-keyword\">get</span> hasAssociatedValues() {\n    <span class=\"hljs-keyword\">return</span> <span class=\"hljs-keyword\">this</span>.associations !== <span class=\"hljs-literal\">null</span>;\n  }\n\n  <span class=\"hljs-comment\">/**\n   * Checks to see if this object is, or is loosely, the same as\n   * the supplied `someCase` value. This is determined by comparing\n   * the `.key` property.\n   *\n   * <span class=\"hljs-doctag\">@param</span> {any} someCase some object value that might have a\n   * matching (double equals) key property value\n   * <span class=\"hljs-doctag\">@returns</span> {boolean} true if the objects are loosely equal (==)\n   * or if each of `.key` values are loosely equal (==)\n   */</span>\n  <span class=\"hljs-keyword\">is</span>(someCase) {\n    <span class=\"hljs-comment\">// noinspection EqualityComparisonWithCoercionJS</span>\n    <span class=\"hljs-keyword\">return</span> <span class=\"hljs-keyword\">this</span> == someCase || <span class=\"hljs-keyword\">this</span>?.key == someCase?.key;\n  }\n\n  <span class=\"hljs-comment\">/**\n   * Define the string representation of any given {<span class=\"hljs-doctag\">@link</span> Enumeration}\n   * instance to be its `.key` value.\n   *\n   * <span class=\"hljs-doctag\">@returns</span> {string} the value of the `.key` property wrapped in\n   * a call to `String()` to ensure conversion.\n   */</span>\n  toString() {\n    <span class=\"hljs-keyword\">return</span> String(<span class=\"hljs-keyword\">this</span>.key);\n  }\n\n  <span class=\"hljs-comment\">/**\n   * Returns a combination of the class&#x27; name followed by this\n   * instances key value. This can be more explicit than just using\n   * the `.key` property.\n   *\n   * <span class=\"hljs-doctag\">@example</span>\n   * class Shape extends Enumeration {\n   *   static {\n   *     Shape.define(&#x27;circle&#x27;)\n   *     Shape.define(&#x27;square&#x27;)\n   *   }\n   * }\n   *\n   * console.log(Shape.circle.case) // &#x27;Shape.circle&#x27;\n   *\n   * // [&#x27;Shape.circle&#x27;, &#x27;Shape.square&#x27;]\n   * console.log([...Shape.values()].map(s =&gt; s.case))\n   *\n   * <span class=\"hljs-doctag\">@type</span> {string}\n   */</span>\n  <span class=\"hljs-keyword\">get</span> case() {\n    <span class=\"hljs-keyword\">return</span> `${<span class=\"hljs-keyword\">this</span>.<span class=\"hljs-keyword\">constructor</span>.name}.${String(<span class=\"hljs-keyword\">this</span>.key)}`\n  }\n\n  <span class=\"hljs-comment\">/**\n   * Define the result of a call to {<span class=\"hljs-doctag\">@link</span> #valueOf} to always be\n   * the contents of the `.value` property.\n   *\n   * <span class=\"hljs-doctag\">@returns</span> {any} the contents of the `.value` property\n   */</span>\n  valueOf() {\n    <span class=\"hljs-keyword\">return</span> <span class=\"hljs-keyword\">this</span>.value\n  }\n\n  <span class=\"hljs-comment\">/**\n   * Returns the `.key` value as a primitive, unless a conversion to\n   * number is requested. In which case, if the `.value` property is\n   * of type {<span class=\"hljs-doctag\">@link</span> Number} then it will be returned. In all other\n   * cases the result will be `String(this.key)`.\n   *\n   * <span class=\"hljs-doctag\">@returns</span> {string|number|NaN} returns a {<span class=\"hljs-doctag\">@link</span> String} representation\n   * of the `.key` property unless a number is requested. See above\n   * for custom logic pertaining to number coercion.\n   */</span>\n  [Symbol.toPrimitive](hint) {\n    switch (hint) {\n      default:\n      case <span class=\"hljs-string\">&#x27;string&#x27;</span>:\n        <span class=\"hljs-keyword\">return</span> String(<span class=\"hljs-keyword\">this</span>.key);\n\n      case <span class=\"hljs-string\">&#x27;number&#x27;</span>:\n        <span class=\"hljs-keyword\">return</span> <span class=\"hljs-keyword\">is</span>.number(<span class=\"hljs-keyword\">this</span>.value)\n          ? <span class=\"hljs-keyword\">this</span>.value\n          : Number(<span class=\"hljs-keyword\">this</span>.key)\n    }\n  }\n\n  <span class=\"hljs-comment\">/**\n   * Generates a custom tag name that matches this instances class name.\n   *\n   * <span class=\"hljs-doctag\">@example</span>\n   * class Shape extends Enumeration {\n   *   static { Shape.define(&#x27;circle&#x27;) }\n   * }\n   *\n   * console.log(Shape.circle[Symbol.toStringTag]) // &#x27;Shape&#x27;\n   */</span>\n  <span class=\"hljs-keyword\">get</span> [Symbol.toStringTag]() {\n    <span class=\"hljs-keyword\">return</span> <span class=\"hljs-keyword\">this</span>.<span class=\"hljs-keyword\">constructor</span>.name;\n  }\n\n  <span class=\"hljs-comment\">/**\n   * Given an instance of a child class of {<span class=\"hljs-doctag\">@link</span> Enumeration}, this code\n   * walks through all valid enum cases for this class. If either present\n   * or missing are functions, they will be invoked and their results will\n   * be what are returned from this match. If they are not functions, then\n   * their direct values will be returned. If they are not supplied, true\n   * is returned if the case is present, false is returned otherwise.\n   *\n   * In the case of present being a function, its signature is expected\n   * to be something like the following. Note that this will be awaited\n   * before returning its result.\n   *\n   * ```js\n   * async present(instance, baseCase, associations) =&gt; Promise&lt;any&gt;\n   * ```\n   * where\n   *  * **instance** is the instance of the enum supplied\n   *  * **baseCase** is the instance of the enum matching `instance`\n   *  * **associations** is either an empty object when there are no custom\n   *    associations, or the associations object itself.\n   *\n   * and missing simply receives the instance passed to `asyncMatch()`,\n   * but if missing is an async function, its call will be awaited before\n   * returning a result.\n   *\n   * ```js\n   * async missing(instance) =&gt; Promise&lt;any&gt;\n   * ```\n   * where\n   *  * **instance** is the instance of the enum supplied\n   *\n   * Some examples:\n   * ```js\n   * Color.match(Color.red) // returns true\n   * Color.match(Color.red, (_,__,{r}) =&gt; r) // returns 255\n   * Color.match(null) // returns false\n   * Color.match(null, (_,__,{r}) =&gt; r, () =&gt; 0) // returns 0\n   * Color.match(null, &#x27;red&#x27;, &#x27;missing&#x27;) // returns &#x27;missing&#x27;\n   *\n   * Color.match(horse.color) // returns true\n   * Color.match(horse.color, instance =&gt; instance.toHex()) // &#x27;#c84c31&#x27;\n   * ```\n   *\n   * <span class=\"hljs-doctag\">@param</span> {Enumeration} instance an instance of any {<span class=\"hljs-doctag\">@link</span> Enumeration}\n   * derived class. With or without associated values.\n   * <span class=\"hljs-doctag\">@param</span> {\n   *   (function(Enumeration,Enumeration,object): Promise&lt;any&gt;|any|undefined)\n   * } present an async function that computes a value or a variable value to\n   * return if the `instance` is one of the known {<span class=\"hljs-doctag\">@link</span> Enumeration} cases\n   * for this subclass. Defaults to `true` if not supplied\n   * <span class=\"hljs-doctag\">@param</span> {function(Enumeration): Promise&lt;any&gt;} missing an async function\n   * that computes a value or a variable value to return if the `instance` is\n   * not one of the known cases for this subclass. Defaults to `false` if not\n   * supplied.\n   * <span class=\"hljs-doctag\">@returns</span> {any} the result of either `present`, or `missing`, using the\n   * default values of `true` and `false`, respectively, should they not be\n   * supplied. The outcome is determined by the existence of the supplied\n   * `instance` object being a known case for this enum subclass.\n   */</span>\n  static async asyncMatch(instance, present, missing) {\n    <span class=\"hljs-keyword\">const</span> resolvePresent = async (caseValue) =&gt; {\n      <span class=\"hljs-keyword\">if</span> (<span class=\"hljs-keyword\">is</span>.function(present)) {\n        <span class=\"hljs-keyword\">const</span> associations = instance?.associations\n          ?? (<span class=\"hljs-keyword\">is</span>.<span class=\"hljs-keyword\">object</span>(instance?.value) ? instance.value : {});\n        <span class=\"hljs-keyword\">return</span> present(instance, caseValue, associations);\n      }\n\n      <span class=\"hljs-keyword\">else</span>\n        <span class=\"hljs-keyword\">return</span> present ?? <span class=\"hljs-literal\">true</span>;\n    }\n\n    <span class=\"hljs-keyword\">const</span> resolveMissing = async () =&gt; {\n      <span class=\"hljs-keyword\">if</span> (<span class=\"hljs-keyword\">is</span>.function(missing))\n        <span class=\"hljs-keyword\">return</span> await missing(instance);\n      <span class=\"hljs-keyword\">else</span>\n        <span class=\"hljs-keyword\">return</span> missing ?? <span class=\"hljs-literal\">false</span>;\n    }\n\n    <span class=\"hljs-keyword\">if</span> (!<span class=\"hljs-keyword\">is</span>.<span class=\"hljs-keyword\">object</span>(instance) || !(instance instanceof <span class=\"hljs-keyword\">this</span>))\n      <span class=\"hljs-keyword\">return</span> await resolveMissing();\n\n    <span class=\"hljs-keyword\">for</span> (<span class=\"hljs-keyword\">const</span> [_, caseValue] of <span class=\"hljs-keyword\">this</span>) {\n      <span class=\"hljs-keyword\">if</span> (instance.key === caseValue.key)\n        <span class=\"hljs-keyword\">return</span> await resolvePresent(caseValue);\n    }\n\n    <span class=\"hljs-keyword\">return</span> await resolveMissing();\n  }\n\n  <span class=\"hljs-comment\">/**\n   * Static variant of {<span class=\"hljs-doctag\">@link</span> Enumeration#is} that takes a left and\n   * right hand value, then checks to see if both objects are, or are\n   * loosely, the same as each other&#x27;s `.key` value.\n   *\n   * <span class=\"hljs-doctag\">@param</span> {any} leftCase some object value that might have a\n   * matching (double equals) key property value\n   * <span class=\"hljs-doctag\">@param</span> {any} rightCase some object value that might have a\n   * matching (double equals) key property value\n   * <span class=\"hljs-doctag\">@returns</span> {boolean} true if the objects are loosely equal (==)\n   * or if each of `.key` values are loosely equal (==)\n   */</span>\n  static <span class=\"hljs-keyword\">is</span>(leftCase, rightCase) {\n    <span class=\"hljs-comment\">// noinspection EqualityComparisonWithCoercionJS</span>\n    <span class=\"hljs-keyword\">return</span> leftCase == rightCase || leftCase?.key == rightCase?.key;\n  }\n\n  <span class=\"hljs-comment\">/**\n   * Used when creating a static instance of {<span class=\"hljs-doctag\">@link</span> Enumeration}. Generally\n   * this is done as follows:\n   *\n   * <span class=\"hljs-doctag\">@example</span>\n   * class Shape extends Enumeration {\n   *   static {\n   *     Shape.define(&#x27;cylinder&#x27;)\n   *     Shape.define(&#x27;cube&#x27;)\n   *     Shade.define(&#x27;other&#x27;)\n   *   }\n   * }\n   *\n   * <span class=\"hljs-doctag\">@param</span> {string|number|symbol} key the case name of the this particular\n   * enumeration instance.\n   * <span class=\"hljs-doctag\">@param</span> {any|[string|number|symbol, any]} value the value of the newly\n   * defined {<span class=\"hljs-doctag\">@link</span> Enumeration} instance.\n   * <span class=\"hljs-doctag\">@param</span> {function|object} [customizeInstance=undefined] defaults to\n   * `undefined`, but when it is passed in as a function, the signature\n   * would be to take an instance of this Enumeration class and return\n   * one (presumably after modification), or in the form of an object whose\n   * property descriptors are copied onto the defined instance. This later\n   * approach retains getter and setter application as well as other rare\n   * descriptor modifications.\n   * <span class=\"hljs-doctag\">@returns</span> {*} an instance of this {<span class=\"hljs-doctag\">@link</span> Enumeration} class type.\n   */</span>\n  static define(key, value = undefined, customizeInstance = undefined) {\n    <span class=\"hljs-keyword\">if</span> (!<span class=\"hljs-keyword\">is</span>.objectKey(key)) {\n      <span class=\"hljs-keyword\">throw</span> new TypeError(\n        <span class=\"hljs-string\">&#x27;Enumeration.define() must have a string/number/symbol key&#x27;</span>\n      );\n    }\n\n    let caseName = key;\n    let caseValue = value ?? key;\n\n    <span class=\"hljs-keyword\">if</span> (<span class=\"hljs-keyword\">is</span>.objectEntry(value)) {\n      caseName = value[<span class=\"hljs-number\">0</span>];\n      caseValue = value[<span class=\"hljs-number\">1</span>];\n    }\n\n    let instance = new <span class=\"hljs-keyword\">this</span>(caseName, caseValue);\n\n    <span class=\"hljs-keyword\">if</span> (customizeInstance instanceof Function) {\n      <span class=\"hljs-keyword\">const</span> newInstance = customizeInstance(instance);\n\n      <span class=\"hljs-keyword\">if</span> (newInstance instanceof <span class=\"hljs-keyword\">this</span>)\n        instance = newInstance;\n    }\n    <span class=\"hljs-keyword\">else</span> <span class=\"hljs-keyword\">if</span> (<span class=\"hljs-keyword\">is</span>.<span class=\"hljs-keyword\">object</span>(customizeInstance)) {\n      <span class=\"hljs-keyword\">const</span> descriptors = Object.getOwnPropertyDescriptors(customizeInstance)\n\n      Object.defineProperties(instance, descriptors)\n    }\n\n    Object.defineProperty(<span class=\"hljs-keyword\">this</span>, key, {\n      <span class=\"hljs-keyword\">get</span>() {\n        <span class=\"hljs-keyword\">return</span> instance;\n      },\n      configurable: <span class=\"hljs-literal\">true</span>,\n      enumerable: <span class=\"hljs-literal\">true</span>,\n    });\n  }\n\n  <span class=\"hljs-comment\">/**\n   * Creates an iterator of all {<span class=\"hljs-doctag\">@link</span> Enumeration} derived instances that\n   * are statically assigned to this class. Generally this is only useful\n   * if applied to child class of `Enumeration`.\n   *\n   * <span class=\"hljs-doctag\">@returns</span> {Generator&lt;string, void, *&gt;} an iterator that walks instances\n   * of derived {<span class=\"hljs-doctag\">@link</span> Enumeration} classes and returns their `.key` values\n   */</span>\n  static *cases() {\n    <span class=\"hljs-keyword\">for</span> (let [key, _] of <span class=\"hljs-keyword\">this</span>) yield key;\n  }\n\n  <span class=\"hljs-comment\">/**\n   * Given an instance of a child class of {<span class=\"hljs-doctag\">@link</span> Enumeration}, this code\n   * walks through all valid enum cases for this class. If either present\n   * or missing are functions, they will be invoked and their results will\n   * be what are returned from this match. If they are not functions, then\n   * their direct values will be returned. If they are not supplied, true\n   * is returned if the case is present, false is returned otherwise.\n   *\n   * In the case of present being a function, its signature is expected\n   * to be something like the following:\n   *\n   * ```js\n   * present(instance, baseCase, associations) =&gt; any\n   * ```\n   * where\n   *  * **instance** is the instance of the enum supplied\n   *  * **baseCase** is the instance of the enum matching `instance`\n   *  * **associations** is either an empty object when there are no custom\n   *    associations, or the associations object itself.\n   *\n   * and missing simply receives the instance passed to `match()` as its\n   * only parameter.\n   *\n   * ```js\n   * missing(instance) =&gt; any\n   * ```\n   * where\n   *  * **instance** is the instance of the enum supplied\n   *\n   * Some examples:\n   * ```js\n   * Color.match(Color.red) // returns true\n   * Color.match(Color.red, (_,__,{r}) =&gt; r) // returns 255\n   * Color.match(null) // returns false\n   * Color.match(null, (_,__,{r}) =&gt; r, () =&gt; 0) // returns 0\n   * Color.match(null, &#x27;red&#x27;, &#x27;missing&#x27;) // returns &#x27;missing&#x27;\n   *\n   * Color.match(horse.color) // returns true\n   * Color.match(horse.color, instance =&gt; instance.toHex()) // &#x27;#c84c31&#x27;\n   * ```\n   * <span class=\"hljs-doctag\">@param</span> {Enumeration} instance an instance of any {<span class=\"hljs-doctag\">@link</span> Enumeration}\n   * derived class. With or without associated values.\n   * <span class=\"hljs-doctag\">@param</span> {\n   *   (function(Enumeration,Enumeration,object): any|any|undefined)\n   * } present a function that computes a value or a variable value to\n   * return if the `instance` is one of the known {<span class=\"hljs-doctag\">@link</span> Enumeration} cases\n   * for this subclass. Defaults to `true` if not supplied\n   * <span class=\"hljs-doctag\">@param</span> {function(Enumeration): any} missing a function\n   * that computes a value or a variable value to return if the `instance` is\n   * not one of the known cases for this subclass. Defaults to `false` if not\n   * supplied.\n   * <span class=\"hljs-doctag\">@returns</span> {any} the result of either `present`, or `missing`, using the\n   * default values of `true` and `false`, respectively, should they not be\n   * supplied. The outcome is determined by the existence of the supplied\n   * `instance` object being a known case for this enum subclass.    */</span>\n  static match(instance, present, missing) {\n    <span class=\"hljs-keyword\">const</span> resolvePresent = (caseValue) =&gt; {\n      <span class=\"hljs-keyword\">if</span> (<span class=\"hljs-keyword\">is</span>.function(present)) {\n        <span class=\"hljs-keyword\">const</span> associations = instance?.associations\n          ?? (<span class=\"hljs-keyword\">is</span>.<span class=\"hljs-keyword\">object</span>(instance?.value) ? instance.value : {});\n        <span class=\"hljs-keyword\">return</span> present(instance, caseValue, associations);\n      }\n\n      <span class=\"hljs-keyword\">else</span>\n        <span class=\"hljs-keyword\">return</span> present ?? <span class=\"hljs-literal\">true</span>;\n    }\n\n    <span class=\"hljs-keyword\">const</span> resolveMissing = () =&gt; {\n      <span class=\"hljs-keyword\">return</span> <span class=\"hljs-keyword\">is</span>.function(missing) ? missing(instance) : (missing ?? <span class=\"hljs-literal\">false</span>);\n    }\n\n    <span class=\"hljs-keyword\">if</span> (!<span class=\"hljs-keyword\">is</span>.<span class=\"hljs-keyword\">object</span>(instance) || !(instance instanceof <span class=\"hljs-keyword\">this</span>))\n      <span class=\"hljs-keyword\">return</span> resolveMissing();\n\n    <span class=\"hljs-keyword\">for</span> (<span class=\"hljs-keyword\">const</span> [_, caseValue] of <span class=\"hljs-keyword\">this</span>) {\n      <span class=\"hljs-keyword\">if</span> (instance.key === caseValue.key)\n        <span class=\"hljs-keyword\">return</span> resolvePresent(caseValue);\n    }\n\n    <span class=\"hljs-keyword\">return</span> resolveMissing();\n  }\n\n  <span class=\"hljs-comment\">/**\n   * Creates an iterator of all {<span class=\"hljs-doctag\">@link</span> Enumeration} derived instances that\n   * are statically assigned to this class. Generally this is only useful\n   * if applied to child class of `Enumeration`.\n   *\n   * <span class=\"hljs-doctag\">@returns</span> {Generator&lt;string, void, *&gt;} an iterator that walks instances\n   * of derived {<span class=\"hljs-doctag\">@link</span> Enumeration} classes and returns their `.value` values\n   */</span>\n  static *values() {\n    <span class=\"hljs-keyword\">for</span> (let [_, value] of <span class=\"hljs-keyword\">this</span>) yield value;\n  }\n\n  <span class=\"hljs-comment\">/**\n   * Creates an iterator of all {<span class=\"hljs-doctag\">@link</span> Enumeration} derived instances that\n   * are statically assigned to this class. Generally this is only useful\n   * if applied to child class of `Enumeration`.\n   *\n   * <span class=\"hljs-doctag\">@returns</span> {Generator&lt;*, void, *&gt;} an iterator that walks instances\n   * of derived {<span class=\"hljs-doctag\">@link</span> Enumeration} classes and returns each key/value pair\n   * as arrays. **This is the same as `Object.entries(ChildEnumerationClass)`\n   * and then filter the results for pairs whose values are instances of\n   * `ChildEnumerationClass`**\n   */</span>\n  static *[Symbol.iterator]() {\n    <span class=\"hljs-keyword\">const</span> keys = Object.keys(<span class=\"hljs-keyword\">this</span>);\n\n    <span class=\"hljs-keyword\">for</span> (<span class=\"hljs-keyword\">const</span> key of keys) {\n      <span class=\"hljs-keyword\">const</span> value = <span class=\"hljs-keyword\">this</span>[key];\n\n      <span class=\"hljs-keyword\">if</span> (value instanceof <span class=\"hljs-keyword\">this</span>) yield [key, value];\n    }\n  }\n}\n\n<span class=\"hljs-comment\">/**\n * Creates a prototype safe {<span class=\"hljs-doctag\">@link</span> Proxy} object, specific to\n * {<span class=\"hljs-doctag\">@link</span> Enumeration} instances, that if a getter tries to reach a named\n * property that doesn&#x27;t exist on the enum, **and** `.hasAssociatedValues`\n * is `true`, then it will attempt to return `this.associations[property]`\n * instead.\n *\n * <span class=\"hljs-doctag\">@param</span> {object} proxied the object to the behavior to; typically this is\n * the prototype of the class itself. See the constructor in Enumeration for\n * usage.\n * <span class=\"hljs-doctag\">@returns</span> {*|null|object} a {<span class=\"hljs-doctag\">@link</span> Proxy} instance around the supplied\n * object to be proxied with the above defined behavior.\n */</span>\nexport function SubscriptProxy(proxied) {\n  <span class=\"hljs-keyword\">const</span> has = (<span class=\"hljs-keyword\">object</span>, property) =&gt; Reflect.has(<span class=\"hljs-keyword\">object</span>, property)\n\n  <span class=\"hljs-keyword\">return</span> new Proxy(proxied, {\n    <span class=\"hljs-keyword\">get</span>(target, property, receiver) {\n      <span class=\"hljs-comment\">// First, if our target does not have a property with the</span>\n      <span class=\"hljs-comment\">// requested, check, in order, the following:</span>\n      <span class=\"hljs-comment\">//   1. The instance&#x27;s associations object.</span>\n      <span class=\"hljs-comment\">//   2. The instance&#x27;s value, if it&#x27;s an object.</span>\n      <span class=\"hljs-keyword\">if</span> (!Reflect.has(target, property)) {\n        <span class=\"hljs-comment\">// If the associations object has a property of a given name</span>\n        <span class=\"hljs-comment\">// return it first</span>\n        <span class=\"hljs-keyword\">if</span> (has((receiver?.associations ?? {}),property))\n          <span class=\"hljs-keyword\">return</span> receiver.associated(property);\n\n        <span class=\"hljs-comment\">// If we are still here and the value is an object and it has</span>\n        <span class=\"hljs-comment\">// a property with the supplied name, return it.</span>\n        <span class=\"hljs-keyword\">if</span> (<span class=\"hljs-keyword\">is</span>.<span class=\"hljs-keyword\">object</span>(receiver.value) &amp;&amp; has(receiver.value, property))\n          <span class=\"hljs-keyword\">return</span> receiver.value[property];\n      }\n\n      <span class=\"hljs-comment\">// If we failed above or in all other cases, provide</span>\n      <span class=\"hljs-comment\">// the default experience for handling property access</span>\n      <span class=\"hljs-keyword\">return</span> Reflect.<span class=\"hljs-keyword\">get</span>(target, property, receiver);\n    },\n\n    <span class=\"hljs-keyword\">set</span>(target, property, newValue, receiver) {\n      <span class=\"hljs-keyword\">if</span> (\n        !Reflect.has(target, property) &amp;&amp;\n        receiver.hasAssociatedValues &amp;&amp;\n        Reflect.has(receiver.associations, property)\n      ) {\n        receiver.associations[property] = newValue\n        <span class=\"hljs-keyword\">return</span> <span class=\"hljs-literal\">true</span>\n      }\n\n      <span class=\"hljs-keyword\">return</span> Reflect.<span class=\"hljs-keyword\">set</span>(target, property, newValue, receiver);\n    },\n  });\n}\n\nexport default Enumeration\n\n<span class=\"hljs-comment\">/**\n * <span class=\"hljs-doctag\">@typedef</span> {object} TypeToolbox\n *\n * <span class=\"hljs-doctag\">@property</span> {function(any): boolean} object a function that takes any value\n * and returns a boolean value if the object is of type `&#x27;object&#x27;` and not null\n * <span class=\"hljs-doctag\">@property</span> {function(any): boolean} function a function that takes any value\n * and returns true if is of type `&#x27;function&#x27;` or is an `instanceof`\n * {<span class=\"hljs-doctag\">@link</span> Function}.\n * <span class=\"hljs-doctag\">@property</span> {function(any): boolean} array a pass-thru to the\n * {<span class=\"hljs-doctag\">@link</span> Array#isArray} function.\n * <span class=\"hljs-doctag\">@property</span> {function(any): boolean} number a function that takes any value\n * and returns `true` if the value is a number, and `false` otherwise.\n * <span class=\"hljs-doctag\">@property</span> {function(any): boolean} objectEntry a function that takes any\n * value and returns true if the object is a 2 element array where the first\n * parameter is one of a string, symbol, or number type. The second parameter\n * can be of any type. If these criteria are not met, false is returned.\n * <span class=\"hljs-doctag\">@property</span> {function(any): boolean} objectKey a function that takes any value\n * and returns true if it is of type symbol, string, or number. Any other type\n * results in a false return value.\n */</span>\n"}};

globalThis[Symbol.for('jsdoc.DocEntry')] = DocEntry;
globalThis[Symbol.for('jsdoc.DocContent')] = DocContent;
globalThis[Symbol.for('jsdoc.content')] = new DocContent(raw);
})();
